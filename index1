<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title>Thu ngân - Karaoke</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #eef2f7;
      margin: 0; padding: 20px;
      text-align: center;
    }
    .card {
      background: white;
      padding: 20px;
      border-radius: 12px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
      max-width: 420px;
      margin: auto;
    }
    h2 {
      color: #333;
      margin-bottom: 20px;
    }
    input, select, button {
      width: 100%;
      padding: 12px;
      margin: 8px 0;
      font-size: 16px;
      border-radius: 8px;
      border: 1px solid #ccc;
      box-sizing: border-box;
    }
    button {
      background: #007bff;
      color: white;
      border: none;
      cursor: pointer;
    }
    button:hover {
      background: #0069d9;
    }
    .list {
      margin-top: 20px;
      text-align: left;
    }
    .list div {
      padding: 8px;
      border-bottom: 1px solid #eee;
    }
  </style>
</head>
<body>
  <div class="card">
    <h2>💵 Thu ngân - Nhập số tiền</h2>

    <label for="room">Chọn phòng:</label>
    <select id="room">
      <option value="Phòng 1">Phòng 1</option>
      <option value="Phòng 2">Phòng 2</option>
      <option value="Phòng 3">Phòng 3</option>
    </select>

    <label for="amount">Nhập số tiền (đ):</label>
    <input type="number" id="amount" placeholder="VD: 500000">

    <button onclick="saveAmount()">💾 Lưu số tiền</button>

    <div class="list" id="roomList"></div>
  </div>

<script>
// 👉 Thay link này bằng link exec của bạn
const API_URL = "https://script.google.com/macros/s/AKfycbyzfRB-aEGHX5Dq9C9_I30E9Mi5HDezEEghsMJDItFDoK-P99w2PiLHkLjSH41SKeJY/exec";

async function saveAmount() {
  const room = document.getElementById("room").value;
  const amount = document.getElementById("amount").value;

  if (!amount) return alert("Vui lòng nhập số tiền!");

  await fetch(API_URL, {
    method: "POST",
    body: JSON.stringify({ action: "setAmount", room: room, amount: amount })
  });

  alert("✅ Đã lưu số tiền cho " + room);
  loadRooms();
}

async function loadRooms() {
  const res = await fetch(API_URL);
  const data = await res.json();

  const list = document.getElementById("roomList");
  list.innerHTML = "<h3>📋 Danh sách phòng:</h3>";

  data.slice(1).forEach(row => {
    const [room, amount, status] = row;
    if (room) {
      list.innerHTML += `<div>${room}: ${amount || 0} đ (${status})</div>`;
    }
  });
}

loadRooms();
</script>
</body>
</html>
